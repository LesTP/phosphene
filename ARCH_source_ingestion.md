# ARCH: Source Ingestion — MOVED

This module has been extracted to the Toolkit project as of 2026-06-02.

**New location:** `toolkit/ARCH_source_ingestion.md` (and `toolkit/src/toolkit/source_ingestion/`).

Phosphene now consumes it via `from toolkit.source_ingestion import ...`. The public API and adapter set are unchanged. See the toolkit ARCH for the current contract.
