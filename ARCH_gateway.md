# ARCH: Gateway — MOVED

This module has been extracted to the Toolkit project as of 2026-06-02.

**New location:** `toolkit/ARCH_gateway.md` (and `toolkit/src/toolkit/gateway/`).

Phosphene now consumes it via `from toolkit.gateway import ...`. The public API is unchanged. See the toolkit ARCH for the current contract; this stub is kept to avoid breaking links and to record where the module went.
